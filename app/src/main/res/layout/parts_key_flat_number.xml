<?xml version="1.0" encoding="utf-8"?>
<layout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
                name="key"
                type="duck.develop.calculator.data.model.entity.Key" />
    </data>
    <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content">
        <TextView
                android:id="@+id/btn_key"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_margin="1dp"
                android:gravity="center"
                android:background="@drawable/selector_btn_key_flat_number"
                android:textStyle="bold"
                android:textColor="@color/selector_text_color_btn_key_flat_number"
                android:textSize="30sp"
                android:text="@{key.display}"
                tools:text="7"/>
    </LinearLayout>
</layout>